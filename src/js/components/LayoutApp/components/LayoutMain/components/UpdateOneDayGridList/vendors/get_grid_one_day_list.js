

export const get_grid_one_day_list = ( day_array ) => {

    let result = [];
    let next_point = 0;
    let last_point = 24 * 60 * 60;

    for( let i = 0; i < day_array.length; i++ ){

    };



    return result;
};